<!-- Mirror of src/nested_par/ — read-only reference; keep in sync with the actual source files. -->

# 11.1 Source code

The complete source for the nested parallelism example. See [Job parallelized with both distributed and shared memory](11-nested-par.md) for the walkthrough.

## C++  (`src/nested_par/cpp/`)

**main.cpp**

```cpp
#include <iostream>
#include <unistd.h>
#include <mpi.h>
#include <omp.h>

int main()
{
    MPI_Init(NULL, NULL);
    
    int rank;
    MPI_Comm_rank(MPI_COMM_WORLD, &rank);
 
    #pragma omp parallel
    {
        int num_thread = omp_get_thread_num();
       
        sleep(num_thread+1);
        std::cout << "Starting thread " << num_thread  <<
                  " on process " << rank << std::endl;
        sleep(1);
        std::cout << "Hello World from thread " << num_thread <<
                  " on process " << rank << std::endl;
        sleep(1);
        std::cout << "Finished thread " << num_thread <<
                  " on process " << rank <<std::endl;
    }
    MPI_Finalize();
 
    return 0;
}
```

**Makefile**

```makefile
CC = mpicxx
CFLAGS = -fopenmp
BINNAME = main.exe

$(BINNAME): main.cpp
	$(CC) -o ./$@ $< $(CFLAGS)

clean:
	rm -f *.exe *.txt *.sh
```

**run_prog.sh**

```bash
#!/usr/bin/env bash
make_run_file() {
cat > $1.sh <<%EOF%
#!/bin/bash
#SBATCH --job-name=$1
#SBATCH --output="out.txt"
#SBATCH --error="err.txt"
#SBATCH --time=00:00:30
#SBATCH --mem=150M
#SBATCH --ntasks-per-node $2
#SBATCH --cpus-per-task $3

make
srun ./main.exe
%EOF%
}

name=nested_cpp
nproc=2
ncpus=2
export OMP_NUM_THREADS=${ncpus}

make_run_file ${name} ${nproc} ${ncpus}
sbatch ${name}.sh
```


## Fortran  (`src/nested_par/fortran/`)

**main.f90**

```fortran
program hello
    USE omp_lib
    implicit none
    include 'mpif.h'
    
    integer :: ierr, rank, num_proc, num_thread!, thread
    
    call MPI_INIT(ierr)
    call MPI_COMM_SIZE(MPI_COMM_WORLD, num_proc, ierr)
    call MPI_COMM_RANK(MPI_COMM_WORLD, rank, ierr)

    !$OMP PARALLEL private(num_thread, thread)

        call sleep(num_thread+1)
        print *, "Starting process ", num_thread, " on process ", rank
        call sleep(1)
        print *, "Hello World from thread ", num_thread, " on process ", rank
        call sleep(1)
        print *, "Finished process ", num_thread, " on process ", rank
    !$OMP END PARALLEL
    call MPI_FINALIZE(ierr)
end program
```

**Makefile**

```makefile
FC = mpif90
FFLAGS = -fopenmp
BINNAME = main.exe

$(BINNAME): main.f90
	$(FC) -o ./$@ $< $(FFLAGS)

clean:
	rm -f *.exe *.txt *.sh
```


## Python  (`src/nested_par/python/`)

**main.py**

```python
from mpi4py import MPI
from multiprocessing import Pool
from time import sleep

comm = MPI.COMM_WORLD
rank = comm.Get_rank()

def hello(i):
    thread = i
    sleep(rank/2)
    print(f"Starting thread {thread} of process {rank}")
    sleep(rank+.75)
    print(f"Hello World from thread {thread} of process {rank}")
    sleep(2-rank)
    print(f"Finished thread {thread} of process {rank}")

n = 2
with Pool(n) as p:
    p.map(hello, range(n))
```
